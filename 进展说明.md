# PalmT9 项目进展说明

> 提交日期：2026-09-05
> 仓库：`stalingalaxy2-ship-it/PalmT9`（私有）

---

## 一、项目一句话定位

PalmT9 是一种**无需实体键盘的视觉手势输入系统**：通过摄像头识别双手关键点，将「拇指捏击指节」的动作映射为诺基亚式 T9 输入，支持字母、数字、符号、候选词与中文拼音，最终把按键映射到电脑原生输入法，实现在**任何输入框**中打字。

核心理念：**手掌本身就是随身携带的触觉键盘**——拇指与指节的真实碰撞，既提供输入动作，也提供本体触觉反馈。

---

## 二、已完成的部分

### 1. 参赛方案与文档（完整）
- `参赛方案_掌上九键PalmT9.md`：17 节完整参赛方案（问题/内核/目标用户/五层架构/Physical AI 深度论证/演示脚本/评分对照/赛程/提交清单/负面清单自检）。
- `发布会发言稿.md`：乔布斯式 6 分钟路演稿，含「物理输出在人体端」的答辩口径。
- `系统架构图.md`：mermaid 数据流 + 关键参数表。
- `演示视频脚本.md`：≤3 分钟分镜 + 拍摄要点 + 现场备用方案。
- `产品手册.md` / `使用说明.md` / `交付清单.md` / `推进计划_团队分工.md`。

### 2. 赛制研究
- 从飞书文档提取了 Anchor 黑客松赛制 `anchor_rules.md`，明确了赛道 A「AI 感知交互装置」的硬门槛（真实传感输入 + 真实物理输出）与评分维度（100 分）。

### 3. 视觉感知层（可用）
- 基于 **MediaPipe HandLandmarker**（新 Tasks API，21 个手部关键点），适配了 mediapipe 1.0.x（旧 `mp.solutions` API 已移除）。
- 模型文件 `hand_landmarker.task`（7.8MB）已本地化，离线可用。

### 4. 触发判定核心逻辑（`gesture_core.py`，已重构）
- 从「单击 + 速度门槛」重构为 **双次捏合状态机**（捏-松-捏-松 = 一次按键），天然防误触。
- 新增 **遮挡补偿**：被捏指腹关键点丢失时，用相邻关节插值兜底。
- 布局已改为：**左手 12 键 T9**（显示英文）+ **右手 10 键数字 0-9**。
- 内置英文 T9 词典 + 中文拼音 T9 词典（离线）。

### 5. 测试基座（部分）
- `test_gesture_core.py` / `test_calibration.py`：单元测试 18 项（词典/候选/退格/标定/触发）。
- `sim.py`：无头仿真框架（合成手部轨迹 → 客观测 TPR/FPR/延迟）。
- `acceptance.py`：真机视觉验收工具（6 项自动判分，生成报告）。
- `calibrate.py`：一键标定（张手测尺度 + 逐键测距离 → 自动算阈值 → 写 config）。

### 6. 工程化交付
- `launcher.py` + `start.bat`：一键启动，首次运行自动标定。
- `config.py`：配置持久化；`logging_setup.py`：运行日志。
- `requirements.txt`；`build_exe.bat`：PyInstaller 打包脚本。
- `.gitignore`：已排除飞书抓取脚本、调试数据、以及 **Insta360 相机 SDK（保密，严禁提交）**。

---

## 三、遇到的技术难点

### 难点 1：触发判定的「误触」与「漏触」矛盾（核心，已解决方向）
- **问题**：单击触发模式下，手静止时摄像头微抖会被当成敲击（误触）；提高速度门槛又导致慢速敲击不触发（漏触）。
- **根因**：用「分数变化量 / 加速度」当速度，手指靠太近时派生量被相邻键污染。
- **解决**：放弃单击 + 速度门槛，改为**双次捏合状态机**——静止手不会产生两次连续捏合，天然防误触；不再依赖速度门槛，也就不会漏触。

### 难点 2：MediaPipe 新版 API 迁移
- pip 安装的 mediapipe 1.0.1 移除了旧 `mp.solutions` API，只剩 Tasks API，且需要单独的 `.task` 模型文件。已适配，并锁版本 `mediapipe>=1.0`。

### 难点 3：标定阈值算法踩坑
- 早期 `recommend_threshold` 用「最难够到的键」的最小距离当基准，导致全局阈值被顶到 0.588，拇指一晃就乱触发。
- 修正：基础阈值只看「容易键」，难键靠 `REACH_FACTOR` 单独放大。

### 难点 4：仿真轨迹失真导致假阴性
- 早期合成轨迹是「直线慢滑」，会刮过相邻键，先触发路过键 + 冷却阻塞真目标，测出大量假阴性。
- 修正：轨迹贴近真实「快速跳变 + 停留」；打字间插抬手间隔。

### 难点 5：遮挡问题（待完成验证）
- 拇指捏指腹时会遮挡目标关键点，MediaPipe 可能丢点/抖动。已在 `gesture_core` 加相邻关节插值兜底，但**尚未在真机验证效果**。

---

## 四、剩余待完成及计划

### 当前状态（诚实说明）
核心逻辑 `gesture_core.py` 已重构为「双次捏合 + 12/10 键布局 + 遮挡补偿」，但**主程序 `palm_t9.py` 和仿真 `sim.py` 尚未同步**，仍是旧版单击逻辑。以下计划按顺序执行。

### 待完成清单

| 优先级 | 任务 | 说明 |
|---|---|---|
| P0 | 重写 `sim.py` 适配双次捏合 | 生成「捏-松-捏-松」轨迹 + 遮挡场景，验证零误触 + 全键触发 |
| P0 | 重写 `palm_t9.py` 适配新逻辑 | 新布局 UI（左手显示英文不显示数字）+ 双次捏合触发 |
| P0 | 系统键盘注入 | 用 `pynput` 发真实键盘事件，实现「任何输入框通用」 |
| P1 | 中英文完整闭环 | 英文=T9→词典→打词；中文=T9→拼音→微软拼音→右手数字选词 |
| P1 | 真机标定 + 验收 | 跑 `calibrate.py` 和 `acceptance.py`，按报告微调参数 |
| P1 | 遮挡真机验证 | 验证相邻关节插值在真实遮挡下的效果 |
| P2 | 录演示视频 | 连续真实盲打 + 指节特写，≤3 分钟 |
| P2 | 填团队分工实名 | 提交「项目说明」所需 |

### 下一步（本次会话继续）
1. 重写 `sim.py`（双次捏合轨迹 + 遮挡仿真），跑通零误触 + 全触发；
2. 重写 `palm_t9.py`（新 UI + 双次捏合 + 键盘注入）；
3. 单元测试全绿后，更新本文档并再次提交 GitHub。
