# 掌上九键 PalmT9

## 让手掌成为 XR 时代随身携带的触觉键盘

> **参赛赛道**：Anchor · 具构（Origin AI Summit 深圳场 · Physical AI）
> **方向**：A｜AI 感知交互装置
> **一句话定位**：当 XR 头显让键盘和触屏「消失」，我们用摄像头 + AI，把手掌本身变成一块摸得着、盲打得了的九键键盘。

---

## 1. 问题：我们解决什么

戴上 XR / AR / VR 之后，用户面临一个根本性断裂：

- **输入工具消失了**。真实世界里的键盘、鼠标、触屏，在头显内不可见、不可触。
- **现有虚拟键盘靠「击空」**。用户在虚空中悬空点击，没有物理按键的确定性，这就是「大猩猩手臂」（Gorilla Arm）效应与「击空感」的来源。
- **认知负荷高、无法盲打**。缺少触觉反馈，用户必须全程注视虚拟键盘，输入慢、易错、易疲劳。

**一句话痛点**：空间计算让人「看见了虚拟世界，却摸不到输入界面」。

**本项目的回答**：与其在虚空里再造一块虚拟键盘，不如把用户**自己的手掌**变成键盘——因为身体是唯一永远在线、零外设、天然带触觉反馈的输入界面。

---

## 2. 内核：为什么是「手掌」

> 本项目真正的内核不是「又一种手势识别」，而是**用本体感受（Proprioception）重建触觉确定性**。

- **本体感受**：人即使不看手，也知道自己每一根手指在哪里——这是肌肉记忆的基础。
- **指节 = 物理按键**：拇指敲击其他手指的指节，会真实地「碰到」一个实体，产生一次**摸得到的确认**。这一次碰撞，就是系统产生的物理反馈。
- **身体是传感器 + 执行器 + 触觉显示器**：摄像头看手（传感输入）→ AI 判定哪一键被按下（决策）→ 拇指撞上指节的那一下真实触感（物理输出）。闭环不在机器端，而在人体端。

这是「具身交互」（Embodied Interaction）才有的能力：**人体本身成为系统闭环的最后一环，因此无需任何外加机械执行器。**

---

## 3. 目标用户

| 用户画像 | 具体场景 | 痛点 |
|---|---|---|
| XR 头显用户（游戏/办公/社交） | 头显内回复消息、搜索、命名文件 | 无键盘可摸、无法盲打 |
| 移动办公 / 通勤人群 | 站立、无桌面、单手操作 | 没有平面放键盘 |
| 弱视 / 无法注视键盘人群 | 需要免视输入的辅助场景 | 依赖触觉而非视觉定位 |
| 极简硬件主义者 | 不想随身携带额外外设 | 手掌即键盘，零外设 |

---

## 4. 方案总览：五层架构

```
┌─────────────────────────────────────────────────────────┐
│                    掌上九键 PalmT9                       │
├──────────────┬──────────────────────────────────────────┤
│ 反馈层        │ 本体触觉(指节碰撞) + 声学点击 + 视觉涟漪    │
│ 优化层        │ T9/NLP 消歧(HMM·Transformer) + 动态阈值    │
│ 映射层        │ 3×3 掌上九键（三指 × 三节）                │
│ 交互层        │ 碰撞判定 + 加减速特征 + 指甲/指腹分发        │
│ 感知层        │ 摄像头 + MediaPipe Hands 21 关键点 + 滤波   │
└──────────────┴──────────────────────────────────────────┘
```

---

## 5. 感知层：摄像头 + 骨架追踪

- **硬件**：单目 USB 摄像头（组委会开发板可直连）。
- **算法**：Google MediaPipe Hands，级联管线（BlazePalm 手掌检测 → 21 个 3D 关键点回归 → 帧间 ROI 预测），毫秒级、可实时。
- **关键点语义**：

| Landmark | 解剖部位 | 交互功能 |
|---|---|---|
| 4 | 拇指尖 | 输入触点（打击锤） |
| 8 / 12 / 16 | 食/中/无名指尖 | 三列按键区 |
| 0 | 腕部 | 全局坐标参考 |

- **噪声抑制**：卡尔曼滤波（预测/更新，动态 Q/R 协方差）+ 指数滑动平均（EMA）混合，消除抖动、保证高动态下轨迹平滑。

---

## 6. 交互层：把「击空」变成「击实」

每次按键 = 拇指尖（Landmark 4）与目标指节在三维空间的**碰撞**：

- **距离判定**：实时计算两点欧氏距离 d，低于动态阈值 τ 判定为触发。
- **防误触**：引入**加减速特征**——有效点击伴随碰撞瞬间的急剧减速与短暂驻留，模拟物理按键的行程感。
- **材质分发（本项目差异化亮点）**：
  - **指甲盖（硬触感）** → 高权重/控制类任务：Shift 大写、删除、功能菜单。
  - **指节指腹（软触感）** → 标准字符输入。

> 人类指尖神经对硬/软材质高度敏感，这条「用材质做逻辑分发」的路径，让盲打无需视觉即可区分「普通键」与「控制键」，进一步强化本体感受的引导作用。

---

## 7. 映射层：掌上 3×3 九键（T9 复兴）

- **三列 = 三根手指**：食指 / 中指 / 无名指。
- **三行 = 三个指节**：指尖 / 远端指节 / 近端指节。
- 完美契合自然握持时拇指的扫掠范围，指节提供**天然物理锚点**，定位远准于虚空「空气键盘」。

| 列（手指） | 第一行·指尖 | 第二行·远端指节 | 第三行·近端指节 |
|---|---|---|---|
| 食指 | 1：. , ? ! | 4：GHI | 7：PQRS |
| 中指 | 2：ABC | 5：JKL | 8：TUV |
| 无名指 | 3：DEF | 6：MNO | 9：WXYZ |

- **T9 逻辑的核心**：以冗余换空间。大按键区域天然高容错，完美对冲视觉算法在复杂光照下的坐标漂移——九十年代为窄屏而生的经典逻辑，今天为「空间抖动」而复兴。

---

## 8. 优化层：AI 让输出「正确」

- **歧义处理**：点击序列「4-2-6」可能对应 HAN / GAN / IBO…… 由 NLP 引擎消歧。
  - **HMM + 维特比解码**：在候选词路径网格中找最高概率路径，融合词频与 N-gram 转移概率。
  - **Transformer 上下文补全**：捕捉长程语义，前文「Going to」出现时，「4-6-6-3」自动首选 home 而非 good。
- **遮挡补偿**：拇指掐按近端指节时易遮挡，用**骨架运动学约束 + 逆运动学（IK）** 反推被遮挡手指形态，保证追踪连续。
- **动态标定**：启动时一次「握拳-张手」，自动计算用户手部缩放系数，动态调整判定阈值 τ——儿童手小自动收紧，成人手大自动放宽，提升通用性。

---

## 9. Physical AI 深度论证（对应评分 25%）

这是本方案能否过关的**核心答辩点**，单独一节讲清楚：

### 9.1 真实传感输入 ✓

摄像头实时采集人手 21 个三维关键点，是**真实世界 → 数字世界**的传感链路，非仿真、非遥控。

### 9.2 AI 真正承重（改变感知 + 决策 + 交互）✓

| 环节 | 谁在做 | AI 是否承重 |
|---|---|---|
| 感知 | MediaPipe 骨架追踪 + 卡尔曼滤波 | AI 从原始像素重建手部语义 |
| 决策 | 碰撞判定 + T9/HMM/Transformer 消歧 | **AI 决定输出哪个字**，而非固定规则 |
| 交互 | 指节碰撞触觉 + 材质分发 | AI 定义了一种全新交互范式 |

→ 不是「纯遥控」、不是「固定规则自动化」、不是「简单接入聊天框」：AI 在感知、决策、交互三层都改变了产品能力。

### 9.3 真实物理输出 ✓（本方案最具争议、也最需要讲透的一点）

**评委可能的质疑**：输出到屏幕，算不算「真实物理输出」？

**我们的论证——物理输出不在机器端，而在人体端：**

1. **人体即执行器**：拇指敲击指节，是一个真实发生的物理动作；系统识别并确认这次碰撞，碰撞本身就是系统「触发」的物理结果。
2. **人体即触觉显示器**：用户真实地「摸到」了每一次按键——这恰恰是本项目相对于「空气键盘」的核心差异（空气键盘击空，本项目击实）。
3. **声学物理输出兜底**：命中按键时通过音箱发出 50ms 机械键盘短促 Click 声——声音是物理世界的声压波，是任何人都无法否认的物理输出。
4. **视觉反馈为辅助**：屏幕涟漪/预发光仅作「虚拟向导」，不承担「物理输出」职责。

> **一句话答辩口径**：传统方案的物理输出在机器端（电机、舵机）；本方案把物理输出放回人体端——AI 识别并确认拇指与指节的碰撞，这个碰撞就是系统产生的真实物理反馈。人体同时是传感器、执行器和触觉显示器。这比任何外接机械都更贴合「具身智能」的定义。

---

## 10. 硬件与部署

| 组成 | 说明 |
|---|---|
| 计算 | 组委会开发板（观点跃迁提供产品与开发套件）或便携电脑 |
| 传感 | 单目 USB 摄像头 |
| 声学输出 | 小型扬声器（Click 音） |
| 视觉输出 | 现场屏幕（模拟 XR 输入框） |
| 可选进阶 | 柔性传感器手套（多模态：视觉定位 + 传感确认压力），作为加分项 |

**BOM 极简**：摄像头 + 开发板 + 小音箱，成本极低、可复现、可量产。

---

## 11. 现场演示脚本（9/6 Demo · 5–8 分钟）

| 时间 | 环节 | 内容 | 目的 |
|---|---|---|---|
| 0:00–0:30 | 开场痛点 | 戴上（示意）头显后键盘消失，虚空点击的击空感 | 建立共情 |
| 0:30–1:00 | 揭晓内核 | 「手掌就是键盘」一句话定位 | 点题 |
| 1:00–2:30 | 现场盲打 | 选手**背对屏幕/不看手**，连续盲打一句完整英文（如 "hello world i am home"） | 证明免视输入，真实连续运行 |
| 2:30–3:30 | 讲清闭环 | 指节碰撞触觉 = 物理输出，播放特写镜头 | 回应负面清单质疑 |
| 3:30–4:30 | 智能消歧 | 现场演示 T9 重码序列被 NLP 正确解码（4-6-6-3 → home） | 证明 AI 承重 |
| 4:30–5:30 | 创新点 | 指甲盖/指腹材质分发：硬触=删除，软触=输入 | 展示差异化 |
| 5:30–6:30 | 指标与展望 | WPM、准确率实测数据 + 落地路径 | 回应评分维度 |

**自主运行 vs 人为控制声明**（赛制要求必须说明）：
- **自主运行**：手势识别、骨架追踪、碰撞判定、T9/NLP 消歧、动态阈值——全部 AI 自主完成。
- **人为输入**：敲击动作本身是用户的输入意图（这是输入，不是「遥控」——用户不是在遥控一台机器，而是在用自己的身体打字）。

---

## 12. 测试与指标（回应「完成度与真实演示」25%）

| 指标 | 定义 | 目标值 |
|---|---|---|
| WPM | 每分钟正确输入单词数 | 训练后 20–30，熟练者 40+ |
| 准确率 | 正确按键 / 总按键（NLP 纠错后） | ≥ 90% |
| 误触率 | 无意图触发 / 总触发 | ≤ 5% |
| 延迟 | 按键→反馈→上屏 | ≤ 100ms |

**测试记录模板**（推荐提交材料）：任务次数、成功/失败、耗时、人工干预次数、失败原因——现场可提供连续 1 分钟盲打录像作为稳定性证据。

---

## 13. 团队分工（模板，按 3–5 人填写）

| 角色 | 职责 |
|---|---|
| 视觉/AI | MediaPipe 骨架追踪、碰撞判定、NLP 消歧 |
| 交互/算法 | 掌上九键映射、材质分发、动态阈值 |
| 产品/场景 | 用户研究、Demo 脚本、路演答辩 |
| 硬件/嵌入式 | 开发板部署、摄像头/音箱接线、现场故障排查 |
| 安全/设备负责人 | 设备登记、现场安全（赛制要求每队指定） |

---

## 14. 评分维度对照表（100 分全映射）

| 评分维度 | 权重 | 本方案对应 |
|---|---|---|
| 技术实现与 Physical AI 深度 | 25% | 感知(骨架追踪)→决策(NLP消歧)→执行(指节碰撞)完整链路，AI 三层承重 |
| 完成度与真实演示 | 25% | 现场连续盲打 + 测试数据 + 稳定性视频 |
| 创新性 | 15% | 身体即键盘、材质分发、T9 复兴于空间计算 |
| 问题真实性与用户价值 | 15% | XR 输入断裂是真实痛点，用户画像具体 |
| 落地潜力 | 15% | BOM 极简、可集成头显/手表、扩展路径清晰 |
| 现场完整度 | 5% | 6 段式演示脚本，时间可控 |

---

## 15. 落地潜力与扩展路径

1. **近场**：作为 XR 头显的原生输入法 SDK，替换虚空键盘。
2. **中场**：集成到智能手表/手环（摄像头 + IMU/sEMG 多模态），实现「视觉看、肌肉感、AI 猜」铁三角。
3. **远场**：通用免视输入——车载、医疗、无障碍辅助，凡「不能看键盘但要打字」的场景皆可。
4. **数据资产**：掌上盲打产生的手部运动数据集，可反哺具身智能手部操控模型。

---

## 16. 赛程执行计划（8/31 – 9/6）

| 时间 | 阶段 | 关键动作 | 交付物 |
|---|---|---|---|
| 8/31 | 开赛 | 确认赛道与组队 | 方向与组队意向 |
| 9/1–9/4 | 线上预研 | 搭 MediaPipe + 碰撞判定 + T9 词典；准备盲打语料；**录好备用连续盲打视频** | 可运行代码 + 设备申请表 |
| 9/5 | 线下 Day1 | 接开发板/摄像头/音箱，真机联调，动态阈值标定 | 最小闭环 Demo |
| 9/6 09:00–12:00 | Final Call | 代码合并、统一测试、录制与提交 | 最终交付包 |
| 9/6 14:00–18:00 | Demo 与颁奖 | 按 6 段式脚本路演 | 主奖与专项奖 |

---

## 17. 提交材料清单

**必交（3 项）**
- [ ] 可运行代码仓库或压缩包（MediaPipe + 碰撞判定 + T9/NLP）
- [ ] ≤3 分钟演示视频（含一段连续真实盲打）
- [ ] 项目说明：问题、目标用户、方案、团队分工（即本文件）

**推荐（3 项）**
- [ ] 系统架构图与硬件连接说明
- [ ] 测试记录：任务次数、成功/失败、耗时、人工干预、失败原因
- [ ] 安全与隐私说明（摄像头采集人脸/手部数据的合规口径）

---

## 附录：负面清单自检

| 负面清单项 | 本方案状态 |
|---|---|
| 纯 App | ✗ 否：开发板 + 摄像头 + 音箱实体装置，非纯软件 |
| 纯策划案 | ✗ 否：现场实时盲打，连续真实运行 |
| 只有渲染图 / 只有 PPT | ✗ 否：实机交互演示 |
| 没有现场实体交互结果演示 | ✗ 否：真人手掌盲打，指节碰撞触觉 + 声学输出 |
| 纯遥控 / 固定规则自动化 | ✗ 否：AI 在感知/决策/交互三层承重 |
