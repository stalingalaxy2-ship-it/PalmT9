# PalmT9 系统架构图

```mermaid
flowchart TD
    subgraph 感知层[感知层 · 真实传感输入]
        CAM[USB 摄像头] --> FLIP[镜像 + 帧同步]
        FLIP --> MP[MediaPipe HandLandmarker<br/>21 关键点 · 毫秒级]
    end

    subgraph 交互层[交互层 · 碰撞判定]
        MP --> THUMB[拇指尖 Landmark 4]
        MP --> KEYS[9 字母键 + 数字键 + 功能键<br/>三指 × 三节]
        THUMB --> DIST[欧氏距离 + 手部尺度归一化]
        KEYS --> DIST
        DIST --> SPEED[拇指真实位移速度<br/>滑窗 SPEED_WINDOW]
        DIST --> DWELL[驻留判定 DWELL_FRAMES=2<br/>防路过误触]
        SPEED --> TRIG[触发判定<br/>距离 + 速度 + 驻留 + 冷却]
        DWELL --> TRIG
    end

    subgraph 映射层[映射层 · T9]
        TRIG --> MAP[左手=字母 T9 九键<br/>右手=数字 0-9<br/>小指=空格/退格/符号]
    end

    subgraph 优化层[优化层 · AI 消歧]
        MAP --> DICT[T9 词典 + 词频排序]
        DICT --> CAND[候选词 top3]
    end

    subgraph 反馈层[反馈层 · 物理输出]
        TRIG --> BEEP[音箱 Click 声<br/>声学物理输出]
        TRIG --> BODY[指节真实碰撞<br/>人体触觉反馈]
        TRIG --> UI[屏幕输入框 + 候选 + 贴骨标签]
    end

    CAND --> UI
    UI --> OUT[上屏输出]
```

## 数据流一句话

```
摄像头 → 21 关键点 → 拇指-指节碰撞(距离/速度/驻留) → T9 键值 → 词典消歧 → 候选上屏
                ↘ 命中即响「哒」声 + 指节真实碰撞感（物理输出闭环）
```

## 关键参数

| 参数 | 值 | 作用 |
|---|---|---|
| `REACH_FACTOR` | 无名指近端 1.5 / 远端 1.15 / 小指 1.1-1.2 | 难够的键放宽阈值 |
| `SPEED_MIN` | 0.08（占手部尺度/帧） | 静止挨着不算敲击 |
| `DWELL_FRAMES` | 2 | 路过不触发 |
| `COOLDOWN` | 0.35s | 防连击 |
| `threshold` | 0.34（可 `[ ]` 调） | 基础触发阈值 |
