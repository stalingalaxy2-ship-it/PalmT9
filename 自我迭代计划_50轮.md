# PalmT9 无监督自我迭代计划（50 轮）

> 目标：在**不依赖摄像头实测**的前提下，对 PalmT9 做 50 轮可验证的自我迭代，
> 每一轮都有明确假设、改动、客观验证、通过标准，改得好就保留、改得差就回滚，
> 全程记录 CHANGELOG，最终产出验收报告 + 迭代总结。

---

## 0. 核心约束与破解思路

**约束**：我无法看你的摄像头，无法实测"手敲一下会不会触发"。

**破解**：把 `palm_t9.py` 的**触发/碰撞判定逻辑抽成纯函数**（输入手部关键点坐标 →
输出触发了哪个键），然后用**合成手部关键点轨迹**喂给它，就能在**无摄像头**下客观测量：

| 指标 | 含义 | 目标 |
|---|---|---|
| TPR 触发率 | 真实敲击被正确识别的比例 | ≥ 95% |
| FPR 误触率 | 手静止放着时误触发的比例 | = 0% |
| 逐键可触率 | 9 字母键 + 数字键 + 功能键每个都能触发 | 100% |
| 延迟 | 单帧处理耗时 | ≤ 20ms |
| 词典覆盖 | 演示句所需词全部可解码 | 100% |
| UI 正确性 | 渲染帧 OCR 校验标签/输入框/候选 | 通过 |

**验证信号三件套**（全程无摄像头）：
1. **无头仿真器**：合成"静息手 / 快速敲 / 慢速敲 / 双手"轨迹，喂给纯判定函数。
2. **单元测试**：`word_to_seq`、候选、状态机、提交/退格逻辑。
3. **渲染 + OCR**：合成帧渲染 UI，用视觉模型读图校验元素是否正确。

**每轮循环**：假设 → 改动 → 跑仿真+单测 → 指标改善则保留并记录 → 否则回滚。

---

## 阶段 0：搭测试基座（第 1–5 轮）

| 轮 | 改动 | 验证 | 通过标准 |
|---|---|---|---|
| 1 | 把触发/碰撞/手势判定从 `main()` 抽成纯函数 `evaluate(landmarks, state) -> events` | 函数可独立 import | 单测可调用，无 cv2 依赖 |
| 2 | 写合成轨迹生成器：静息手、快速敲、慢速敲、双手、抖动 | 能产出合法 21 点序列 | 轨迹可复现 |
| 3 | 写指标计算：TPR/FPR/逐键率/延迟 | 对合成轨迹出分 | 指标数值合理 |
| 4 | 跑基线，存 `baseline_report.json` | 生成基线 | 各指标有基线值 |
| 5 | 写迭代器：自动跑测试、对比基线、keep/revert、写 CHANGELOG | 空跑一轮成功 | CHANGELOG 有条目 |

## 阶段 1：触发判定调优（第 6–25 轮）——纯客观，提升最大

| 轮 | 主题 | 具体 |
|---|---|---|
| 6–10 | 阈值 + REACH_FACTOR 逐键网格搜索 | 用仿真器对每个键扫阈值，选 FPR=0 且 TPR 最高的组合 |
| 11–14 | APPROACH_MIN / 冷却 / 防连击 / 重新武装 调参 | 网格搜索，目标是静息 FPR=0 且慢敲 TPR≥95% |
| 15–18 | 敲击模型升级：接近速度 + 短暂驻留 + 加速度 | 仿真"静息/滑过/真敲"三类轨迹验证区分度 |
| 19–21 | 手部尺度归一化 + 卡尔曼/EMA 平滑 | 不同手距、加噪轨迹下 TPR/FPR 稳定 |
| 22–25 | 边界鲁棒性：单手/双手切换、手进出画面、遮挡 | 仿真这些异常，不崩、不误触 |

## 阶段 2：输入引擎（第 26–35 轮）

| 轮 | 主题 | 具体 |
|---|---|---|
| 26–28 | 扩词典 + 词频优化 | 覆盖演示句全部词；单测验证解码正确 |
| 29–31 | 多候选轮播/选择 | 状态机支持切换候选，单测通过 |
| 32–33 | 中文：拼音→汉字 离线词表 | 架构同英文 T9（两级消歧），单测验证 |
| 34–35 | N-gram/上下文消歧 | 简单二元模型，演示句首选词正确率提升 |

## 阶段 3：UI/UX（第 36–42 轮）——渲染帧 OCR 验证

| 轮 | 主题 | 具体 |
|---|---|---|
| 36–38 | 极简 UI 微调（字号/位置/对比度/留白） | 渲染帧 OCR 校验无重叠、无裁切 |
| 39–40 | 自动标定（握拳-张手→自动算阈值） | 仿真不同手大小，阈值自适应正确 |
| 41–42 | 新手引导浮层 + 键位图例 | 渲染 OCR 校验可读 |

## 阶段 4：交付物（第 43–50 轮）

| 轮 | 主题 | 具体 |
|---|---|---|
| 43 | README（怎么跑） | 换机可复现 |
| 44 | requirements.txt 锁定版本 | pip 可装 |
| 45 | 系统架构图（文字/mermaid） | 与代码一致 |
| 46 | 演示视频逐秒脚本 | 对齐发布会发言稿 |
| 47 | 中文输入注入（multi-tap 拼音→系统 IME） | 单测注入事件序列正确 |
| 48 | 提交包组装（代码/模型/文档/测试记录） | 清单齐全 |
| 49 | 全量回归 + 验收报告 | 仿真 6 项全绿 |
| 50 | 迭代总结：50 轮 CHANGELOG + 前后指标对比 | 输出总结文档 |

---

## 执行机制

- **每轮必须留痕**：`iterations/NN_主题.md` 记录假设/改动/指标前后值/保留或回滚。
- **指标看板**：`metrics.json` 持续更新 TPR/FPR/逐键率/延迟。
- **回滚纪律**：任何一轮让 FPR>0 或 TPR 下降，立即回滚该改动。
- **无监督边界**：凡是必须真人实测的（手感、光照、摄像头角度），标记为"待现场校准"，不强行迭代。

## 风险与边界

- 仿真 ≠ 真实手感：阈值等最终仍需现场用 `acceptance.py` 校一遍（已备好）。
- 中文注入、N-gram 若超时，可降级为离线词典，不阻塞主线。
