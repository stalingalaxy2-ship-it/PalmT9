# PalmT9 迭代总结与验收报告

## 一句话结论

PalmT9 从「跑通最小 MVP」迭代到「触发判定在无头仿真下 22/22 全触发、零误触、端到端 hello 全绿」，并补齐了完整交付包。

## 本轮完成了什么

1. **无摄像头可验证的测试基座**：`gesture_core.py`（纯逻辑）+ `sim.py`（合成轨迹 + 客观指标）。
2. **触发判定算法重写**：从「score 变化量」改为「拇指真实位移速度 + 接触瞬间锁存 + 驻留≥2帧」，解决误触与漏触。
3. **双手自敲布局落地**：左手字母九键、右手数字、小指功能键，同步到 `palm_t9.py` 与 `acceptance.py`。
4. **交付包补齐**：README、requirements、架构图、演示视频脚本、CHANGELOG。
5. **429 重试调优**：DSH `settings.yaml` 四个 provider 的 `maxRetries` 全部设为 10000。

## 最终指标（仿真）

- 逐键触发 TPR：**22/22 (100%)**
- 静息误触 FPR：**0**
- 端到端盲打 hello：**ok=True**
- 单帧处理延迟：**0.010 ms**

## 仍需现场人工确认（我无法代做的）

| 事项 | 原因 |
|---|---|
| 真机手感 | 阈值/速度/驻留参数在仿真下最优，真机需按 `acceptance_report.txt` 微调 |
| 左右手识别 | 依赖屏幕左右位置，现场若反了按 `h` 交换 |
| 演示视频录制 | 需真人连续盲打，无法代录 |
| DSH 重启生效 429 配置 | settings.yaml 需重启 DSH |

## 提交清单自检

- [x] 可运行代码 `palm_t9.py`
- [x] 模型 `hand_landmarker.task`
- [x] README + requirements.txt
- [x] 系统架构图
- [x] 项目说明 `参赛方案_掌上九键PalmT9.md`
- [x] 发布会发言稿
- [x] 测试记录（metrics.json / acceptance_report.txt / CHANGELOG）
- [ ] 演示视频（需真人录制）
- [ ] 团队分工实名（需用户填写）
