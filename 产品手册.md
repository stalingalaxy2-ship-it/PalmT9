# PalmT9 产品手册

## 这是什么

PalmT9 是一套**基于计算机视觉的无键盘盲打系统**：摄像头识别你的双手 21 个关键点，你用**拇指敲自己的指节**输入——左手敲字母（T9 九键）、右手敲数字、小指敲空格/退格/符号/切换候选。敲到指节的那一下真实碰撞感，就是系统的物理反馈。

## 首次使用（标定）

1. 双击 `start.bat`（或 `python launcher.py`）。
2. 首次运行会自动进入**标定流程**：
   - 双手张开静置 2 秒 → 测手部尺度；
   - 按提示逐个敲左手 9 个字母键 + 空格 + 退格；
   - 系统自动算出最合适的触发阈值，写入 `config.json`。
3. 之后每次启动直接进入输入界面，**无需再调参**。

> 想重新标定：双击运行 `python calibrate.py`。

## 键位

| 手 | 手指 | 指尖 | 远端(第二节) | 近端(第三节) |
|---|---|---|---|---|
| 左手(字母) | 食指 | 1 .,?! | 4 GHI | 7 PQRS |
| 左手(字母) | 中指 | 2 ABC | 5 JKL | 8 TUV |
| 左手(字母) | 无名指 | 3 DEF | 6 MNO | 9 WXYZ |
| 右手(数字) | 食指 | 1 | 4 | 7 |
| 右手(数字) | 中指 | 2 | 5 | 8 |
| 右手(数字) | 无名指 | 3 | 6 | 9 |

小指：
- 左手小指：**尖 = 空格/确认**，远端 = **退格/删除**，近端 = **切换候选**
- 右手小指：**尖 = 数字 0**，远端 = **符号**（. , ! ? 循环），近端 = **大小写(Shift)**

## 怎么用

1. **打单词**：左手拇指敲左手关节，候选词条出现，用**左小指近端**切换候选，**左小指尖**确认上屏。
2. **打大写**：先敲**右小指近端(Shift)**，再打单词，首字母自动大写（一次性）。
3. **打中文**：按键盘 `m` 切到中文模式，左手敲拼音（如 nihao=6,4,4,2,6），空格出「你好」。
4. **打数字**：右手拇指敲右手关节，直接上屏。
5. **删错**：左小指远端（退格）。
6. **打符号**：右小指远端循环标点。

## 运行中按键

| 键 | 作用 |
|---|---|
| `[` / `]` | 临时调小/调大阈值（本次会话有效） |
| `h` | 交换左右手（键位标反时） |
| `m` | 切换中英文输入 |
| `c` | 清空输入 |
| `ESC` | 退出 |

## 测试与排错

| 命令 | 作用 |
|---|---|
| `python sim.py` | 无头仿真测试（不需要摄像头） |
| `python acceptance.py` | 视觉验收（需要摄像头） |
| `python -m pytest test_gesture_core.py -v` | 单元测试 |
| `palm_t9.log` | 运行日志（出问题发给我） |
| `config.json` | 标定结果（可手动改 threshold） |

## 常见问题

| 现象 | 解决 |
|---|---|
| 键位标反手 | 按 `h` 交换 |
| 敲了没反应 | 按 `]` 调松阈值，或重新标定 |
| 乱触发 | 按 `[` 调紧阈值，或重新标定 |
| 看不到骨架 | 补光、纯色背景、手离摄像头 30-50cm |
