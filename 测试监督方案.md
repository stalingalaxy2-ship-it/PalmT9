# PalmT9 测试监督方案（QA Supervision Plan）

## 角色
开发侧完成功能 → 本测试体系独立验收 → 不合格打回，合格进入交付清单核对。

## 测试分层

| 层 | 工具 | 是否需要摄像头 | 触发方式 |
|---|---|---|---|
| L1 单元测试 | test_product_qa.py / test_gesture_core.py / test_calibration.py | 否 | 每次提交必跑 |
| L2 无头仿真 | 合成21关键点双次捏合轨迹，逐键触发率/误触率/延迟 | 否 | 每次提交必跑 |
| L3 视觉验收 | acceptance.py 实机 + 截图人工/视觉模型复核 | 是 | 交付前 |
| L4 交付门禁 | qa_gate.py 一键出报告 qa_report.md | 否 | 交付前 |

## 覆盖点
- 状态完整性：GestureState 字段齐全、evaluate_frame 容错（坏帧/空帧不崩）
- 双次捏合状态机：22 键逐键触发、单次捏合不触发、超时不触发、冷却、双手独立
- 误触底线：静息手+抖动 100 帧 ×2 手 = 0 事件
- 文本管线：T9 解码、候选排序/切换、退格优先级、Shift 一次性、中文拼音
- 标定：尺度估计、阈值推荐夹取
- 性能：单帧判定 < 1ms
- 交付物：13 个文件齐全、模型文件 >1MB、config.json 合法

## 当前验收结论（首轮）
- 通过 29 项 / 失败 30 项 → **不通过，打回**
- P0: GestureState 未初始化 `_pinch`，evaluate_frame 第一帧即 AttributeError，
  导致全部触发类用例崩溃（主程序只要有手入镜就崩）
- P1: gesture_core.py 内多处 `... if False else ...` 死代码（第254/269/272行），
  说明重构未完成，需清理
- P1: sim.py 仍引用旧 API（LEFT_LETTERS/LEFT_SPACE/事件payload字段），已无法运行，需同步重构
- P1: test_gesture_core.py 中旧驻留模型的用例与新双次捏合语义不符，需更新

## 修复后回归流程
1. 开发说完工 → 运行 `python qa_gate.py`
2. 全绿 → 运行 `python acceptance.py` 做摄像头视觉验收，截图存档
3. 验收通过 → 核对交付清单.md → build_exe.bat 打包 → 干净机器冒烟
